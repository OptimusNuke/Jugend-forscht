package setup.Sensors;

/**
 * @author Tim_Wieland
 * @version 1.0.0
 *
 *    This class: - Checks, if the given port isn't already used as a port
 *    			  - otherwise, there would be an error.
 * 
 */

public class SensorChecklist {
	/*
	private static List<String> usedPorts = new ArrayList<String>();

	/**
	 * 
	 * @param port COM-Port, which is given
	 * 
	 *             This method checks, if the port is usable. Otherwise it will
	 *             print an error-text.
	 *//*
	public static void checkIfUsable(String port) {
		if (!usedPorts.contains(port)) {
			usedPorts.add(port);
		} else {
			System.err.printf("WARNING! USB-Port %s is already used!", port);
		}
	}

	public static int getListLength() {
		return usedPorts.size();
	}
	*/
}
